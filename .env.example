# Database Configuration
# PostgreSQL connection string - update with your credentials
DATABASE_URL="postgresql://masterlist_user:masterlist_password123@localhost:5432/masterlist"

# Authentication
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
SESSION_SECRET="your-session-secret-change-this-in-production"

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Origins (comma-separated)
CORS_ORIGINS="http://localhost:3000,http://localhost:5173"

# Optional: OpenAI API for AI insights (not required for basic functionality)
# OPENAI_API_KEY="your-openai-api-key"

# Optional: Email Configuration (for notifications)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"

# Optional: File Upload Configuration
# UPLOAD_DIR="./uploads"
# MAX_FILE_SIZE="10485760" # 10MB in bytes

# Optional: Redis Configuration (for caching)
# REDIS_URL="redis://localhost:6379"

# Optional: Analytics
# GA_TRACKING_ID="G-XXXXXXXXXX"
# MIXPANEL_TOKEN="your-mixpanel-token"

# Logging Configuration
LOG_LEVEL="info"  # debug, info, warn, error
# Enable debug endpoints in production (not recommended)
# ALLOW_DEBUG="true"