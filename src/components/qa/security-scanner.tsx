'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export function SecurityScanner() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Security Scanner</CardTitle>
        <CardDescription>AI-powered vulnerability detection and recommendations</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Security scanner component - Coming soon</p>
      </CardContent>
    </Card>
  );
}