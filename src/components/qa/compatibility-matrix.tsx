'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export function CompatibilityMatrix() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Compatibility Matrix</CardTitle>
        <CardDescription>Cross-browser and device compatibility testing</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Compatibility matrix component - Coming soon</p>
      </CardContent>
    </Card>
  );
}